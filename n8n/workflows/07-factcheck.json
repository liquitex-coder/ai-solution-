{
  "name": "07 ファクトチェック記者",
  "nodes": [
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "cronExpression",
              "expression": "0 22 * * *"
            }
          ]
        }
      },
      "id": "wf07node00-0000-4000-8000-000000000000",
      "name": "トリガー（daily）",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [
        240,
        300
      ]
    },
    {
      "parameters": {
        "jsCode": "// Example input so a manual run produces a real draft. Replace with your real source.\nreturn [{ json: { rawSource: {\"claims\":[{\"text\":\"新AIモデルXは推論速度がGPT-4の10倍\",\"source\":\"example-startup blog\",\"url\":\"https://example.com/x\",\"verdict\":\"MISLEADING\",\"evidence\":\"10倍は特定の短文プロンプト・キャッシュ有効時のみ。標準ベンチでは約1.4倍。\"}]} } }];"
      },
      "id": "wf07node01-0000-4000-8000-000000000000",
      "name": "ソース入力（例データ・要差し替え）",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        460,
        300
      ]
    },
    {
      "parameters": {
        "jsCode": "const GITHUB_TOKEN = $env.GITHUB_TOKEN || '';\nconst PROMPT_FILE = '07-factcheck.md';\nconst promptUrl = `https://api.github.com/repos/liquitex-coder/ai-solution-/contents/n8n/prompts/${PROMPT_FILE}`;\nconst headers = { 'Accept': 'application/vnd.github.v3+json', 'User-Agent': 'n8n-ai-navi/1.0' };\nif (GITHUB_TOKEN) headers['Authorization'] = `token ${GITHUB_TOKEN}`;\nlet promptContent = '';\ntry {\n  const res = await fetch(promptUrl, { headers });\n  if (res.ok) { const data = await res.json(); promptContent = Buffer.from((data.content || '').replace(/\\n/g, ''), 'base64').toString('utf8'); }\n} catch (e) { promptContent = '<!-- prompt fetch failed -->'; }\nreturn [{ json: { ...$json, promptContent } }];"
      },
      "id": "wf07node02-0000-4000-8000-000000000000",
      "name": "プロンプト読込み (WF-07)",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        680,
        300
      ]
    },
    {
      "parameters": {
        "jsCode": "// AUTO-GENERATED from reporters/reporters/07-factcheck.mjs — do not edit; run \"npm run gen:n8n\"\nconst meta = {\"id\":\"07\",\"slug\":\"factcheck\",\"title\":\"ファクトチェック記者\",\"category\":\"ファクトチェック\",\"categorySlug\":\"factcheck\",\"model\":\"claude-sonnet-5\",\"trigger\":\"daily\"};\nfunction baseClaudeRequest({ model, system, user, maxTokens = 2048 }) {\n  if (!model) throw new Error('model is required');\n  if (!user) throw new Error('user content is required');\n  return {\n    model,\n    max_tokens: maxTokens,\n    system: system || '',\n    messages: [{ role: 'user', content: user }],\n  };\n}\nfunction normalize(raw) {\n  if (!raw || !Array.isArray(raw.claims) || raw.claims.length === 0) {\n    throw new Error('factcheck: rawSource.claims must be a non-empty array');\n  }\n  return raw.claims.map((c) => ({\n    claim: String(c.text).trim(),\n    source: String(c.source).trim(),\n    url: c.url || null,\n    verdict: c.verdict || 'UNVERIFIED', // supplied by Claim-Auditor upstream\n    evidence: c.evidence || '',\n  }));\n}\nfunction buildClaudeRequest({ items, prompt, model }) {\n  const user = `${prompt}\\n\\n## 検証対象クレーム（判定は確定済み・改変禁止）\\n${JSON.stringify(items, null, 2)}`;\n  return baseClaudeRequest({ model: model || meta.model, system: prompt, user });\n}\nconst rawSource = $json.rawSource;\nconst prompt = $json.promptContent || '';\nconst items = normalize(rawSource);\nconst claudeBody = buildClaudeRequest({ items, prompt, model: meta.model });\nreturn [{ json: { claudeBody } }];"
      },
      "id": "wf07node03-0000-4000-8000-000000000000",
      "name": "リクエスト生成 (normalize+buildClaudeRequest)",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        900,
        300
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://api.anthropic.com/v1/messages",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "body": {
          "contentType": "json",
          "jsonBody": "={{ $json.claudeBody }}"
        },
        "headers": {
          "parameters": [
            {
              "name": "anthropic-version",
              "value": "2023-06-01"
            }
          ]
        }
      },
      "id": "wf07node04-0000-4000-8000-000000000000",
      "name": "Claude API",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1120,
        300
      ],
      "credentials": {
        "httpHeaderAuth": {
          "id": "CLAUDE_API_CRED_ID",
          "name": "Claude API Key"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "// AUTO-GENERATED from reporters/reporters/07-factcheck.mjs — do not edit; run \"npm run gen:n8n\"\nconst meta = {\"id\":\"07\",\"slug\":\"factcheck\",\"title\":\"ファクトチェック記者\",\"category\":\"ファクトチェック\",\"categorySlug\":\"factcheck\",\"model\":\"claude-sonnet-5\",\"trigger\":\"daily\"};\nconst FORBIDDEN_TAGS = [\"html\",\"body\",\"head\",\"script\",\"style\",\"doctype\"];\nconst FORBIDDEN_PHRASES = [\"おそらく\",\"かもしれません\",\"と思われます\",\"と思います\"];\nconst DISCLOSURE_KINDS = [\"none\",\"affiliate\",\"sponsored\",\"ad\"];\nconst DISCLOSURE_NOTICES = {\"affiliate\":\"本記事にはアフィリエイトリンクを含みます\",\"sponsored\":\"本記事はスポンサーの提供でお届けします\",\"ad\":\"本記事には広告を含みます\"};\nconst DISCLOSURE_HEAD_WINDOW = 400;\nfunction extractText(claudeResponse) {\n  if (!claudeResponse || !Array.isArray(claudeResponse.content)) {\n    throw new Error('claudeResponse.content must be an array');\n  }\n  return claudeResponse.content\n    .filter((block) => block && block.type === 'text' && typeof block.text === 'string')\n    .map((block) => block.text)\n    .join('')\n    .trim();\n}\nfunction titleFromH2(html) {\n  const match = /<h2[^>]*>([\\s\\S]*?)<\\/h2>/i.exec(html || '');\n  if (!match) return '';\n  return match[1].replace(/<[^>]+>/g, '').trim();\n}\nfunction baseWpPayload({ title, html, category, categorySlug, tags = [], status = 'draft', reporterId, disclosure = 'none', revenueLinks = [] }) {\n  return {\n    title,\n    content: html,\n    status,\n    categoryName: category || null,\n    categorySlug: categorySlug || null,\n    tagNames: tags,\n    // disclosure/revenue_links feed the §16-4 gate (assertDisclosure); every\n    // aggregation reporter defaults to 'none' — a monetized article must opt in.\n    meta: {\n      generated_by: 'ai-reporter',\n      reporter_id: reporterId || null,\n      disclosure,\n      revenue_links: revenueLinks,\n    },\n  };\n}\nfunction assertArticleHtml(html) {\n  if (typeof html !== 'string' || html.trim().length === 0) {\n    throw new Error('article html is empty');\n  }\n  const body = html.trim();\n  if (!/^<h2[\\s>]/i.test(body)) {\n    throw new Error('article must start with an <h2> tag');\n  }\n  if (body.includes('```')) {\n    throw new Error('article must not contain markdown code fences (```)');\n  }\n  for (const tag of FORBIDDEN_TAGS) {\n    if (new RegExp(`<${tag}[\\\\s>/]`, 'i').test(body)) {\n      throw new Error(`article must not contain a <${tag}> tag`);\n    }\n  }\n  for (const phrase of FORBIDDEN_PHRASES) {\n    if (body.includes(phrase)) {\n      throw new Error(`article contains forbidden phrase: ${phrase}`);\n    }\n  }\n  return true;\n}\nfunction assertDisclosure(payload) {\n  const meta = payload.meta || {};\n  const disclosure = meta.disclosure;\n  if (!DISCLOSURE_KINDS.includes(disclosure)) {\n    throw new Error(`meta.disclosure must be one of ${DISCLOSURE_KINDS.join('|')}, got: ${disclosure}`);\n  }\n  const links = Array.isArray(meta.revenue_links) ? meta.revenue_links : [];\n  if (disclosure === 'none') {\n    if (links.length > 0) {\n      throw new Error('meta.revenue_links present but disclosure is \"none\" — undeclared monetization');\n    }\n    return true;\n  }\n  const body = String(payload.content || '');\n  const notice = DISCLOSURE_NOTICES[disclosure];\n  if (!body.slice(0, DISCLOSURE_HEAD_WINDOW).includes(notice)) {\n    throw new Error(`disclosure \"${disclosure}\" requires the opening notice \"${notice}\" within the first ${DISCLOSURE_HEAD_WINDOW} chars`);\n  }\n  if (links.length === 0) {\n    throw new Error(`disclosure \"${disclosure}\" requires a non-empty meta.revenue_links list`);\n  }\n  const allAnchors = body.match(/<a\\s[^>]*>/gi) || [];\n  for (const url of links) {\n    const anchors = allAnchors.filter((a) => a.includes(`href=\"${url}\"`));\n    if (anchors.length === 0) {\n      throw new Error(`revenue link ${url} is declared but not present as an <a> tag in the body`);\n    }\n    for (const a of anchors) {\n      const rel = /rel=\"([^\"]*)\"/i.exec(a);\n      const relVal = rel ? rel[1] : '';\n      if (!/\\bsponsored\\b/.test(relVal) || !/\\bnofollow\\b/.test(relVal)) {\n        throw new Error(`revenue link ${url} must carry rel=\"sponsored nofollow\" (G3), got: ${relVal || '(no rel)'}`);\n      }\n    }\n  }\n  return true;\n}\nfunction assertWpPayload(payload) {\n  if (!payload || typeof payload !== 'object') {\n    throw new Error('wp payload must be an object');\n  }\n  if (typeof payload.title !== 'string' || payload.title.trim().length === 0) {\n    throw new Error('wp payload title is empty');\n  }\n  if (payload.status !== 'draft' && payload.status !== 'publish') {\n    throw new Error(`wp payload status must be draft|publish, got: ${payload.status}`);\n  }\n  // Safe-by-default: anything not explicitly 'publish' must be a draft.\n  assertArticleHtml(payload.content);\n  // Monetization must be declared and properly attributed (§16-4 G1–G3).\n  assertDisclosure(payload);\n  return true;\n}\nfunction parseArticle(claudeResponse) {\n  const html = extractText(claudeResponse);\n  return { title: titleFromH2(html) || 'ファクトチェック', html };\n}\nfunction buildWpPayload(article, opts = {}) {\n  return baseWpPayload({\n    title: article.title,\n    html: article.html,\n    category: meta.category,\n    categorySlug: meta.categorySlug,\n    tags: opts.tags || ['ファクトチェック', '検証'],\n    reporterId: meta.id,\n  });\n}\nconst claudeResponse = $json;\nconst article = parseArticle(claudeResponse);\nconst wpPayload = buildWpPayload(article);\nassertWpPayload(wpPayload); // reject malformed article before posting\nreturn [{ json: wpPayload }];"
      },
      "id": "wf07node05-0000-4000-8000-000000000000",
      "name": "記事生成 (parseArticle+buildWpPayload+validate)",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1340,
        300
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://public-api.wordpress.com/wp/v2/sites/liquitex929aa21393-eyqci.wordpress.com/posts",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "body": {
          "contentType": "json",
          "jsonBody": {
            "title": "={{ $json.title }}",
            "content": "={{ $json.content }}",
            "status": "={{ $json.status }}"
          }
        }
      },
      "id": "wf07node06-0000-4000-8000-000000000000",
      "name": "WordPressに下書き投稿",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1560,
        300
      ],
      "credentials": {
        "httpHeaderAuth": {
          "id": "WORDPRESS_APP_PASSWORD_CRED_ID",
          "name": "WordPress App Password"
        }
      }
    },
    {
      "parameters": {},
      "id": "wf07node09-0000-4000-8000-000000000000",
      "name": "エラートリガー",
      "type": "n8n-nodes-base.errorTrigger",
      "typeVersion": 1,
      "position": [
        240,
        600
      ]
    }
  ],
  "connections": {
    "トリガー（daily）": {
      "main": [
        [
          {
            "node": "ソース入力（例データ・要差し替え）",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "ソース入力（例データ・要差し替え）": {
      "main": [
        [
          {
            "node": "プロンプト読込み (WF-07)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "プロンプト読込み (WF-07)": {
      "main": [
        [
          {
            "node": "リクエスト生成 (normalize+buildClaudeRequest)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "リクエスト生成 (normalize+buildClaudeRequest)": {
      "main": [
        [
          {
            "node": "Claude API",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Claude API": {
      "main": [
        [
          {
            "node": "記事生成 (parseArticle+buildWpPayload+validate)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "記事生成 (parseArticle+buildWpPayload+validate)": {
      "main": [
        [
          {
            "node": "WordPressに下書き投稿",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1"
  },
  "tags": [
    {
      "name": "AI記者"
    },
    {
      "name": "ファクトチェック"
    }
  ],
  "notes": "AUTO-GENERATED from reporters/reporters/07-factcheck.mjs. Do not edit Code nodes by hand — edit the module and run \"npm run gen:n8n\".\n\nRequired credentials: Claude API Key (httpHeaderAuth: x-api-key), WordPress App Password (httpHeaderAuth: Authorization Basic).\nPrompt is loaded from n8n/prompts/07-factcheck.md at runtime.\nThe \"ソース入力\" node holds example data so a manual run yields a real draft; replace it with your real source."
}
