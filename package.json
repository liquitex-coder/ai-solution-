{
  "name": "ai-solution",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "AIナビ — auto AI media pipeline. Reporter framework (no runtime deps).",
  "scripts": {
    "test": "node --test reporters/*.test.mjs",
    "check:reporters": "node scripts/check_reporters.mjs",
    "reporters:dry-run": "node reporters/run.mjs",
    "gen:n8n": "node scripts/gen_n8n.mjs",
    "e2e": "node scripts/e2e_smoke.mjs",
    "gate": "npm test && npm run check:reporters"
  },
  "engines": {
    "node": ">=18"
  }
}
